version: '3.8'

services:
  app:
    build:
      context: .
      target: builder
    environment:
      NODE_ENV: production
    volumes:
      - .:/app
      - /app/node_modules
    command: node --watch server.js
